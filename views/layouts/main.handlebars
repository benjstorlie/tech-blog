<!DOCTYPE html>
<html lang="en">

<head>
  <title>Tech Blog Prototype</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
   <link rel="icon" type="image/png" sizes="16x16 " href="/images/tech-blog-logo-16x16.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/images/tech-blog-logo-32x32.png">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css" />

</head>

<body>
  <div class="container-fluid vw-100">
    <header class="row py-3">
      <div id="header-logo" class="col-12 col-md-4">
        <img id="logo" src="/images/tech-blog-logo.png" alt="logo">
        <img id="logo-text" src="/images/tech-blog-logo-text.png" alt="tech blog">
      </div>
      <nav class="col-12 col-md-8">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">profile</a> |
        <button class="btn" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
        <ul class="nav justify-content-end">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">New Post</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">My Profile</a>
          </li>
        </ul>
      </nav>
    </header>
    <main class="row p-3">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
